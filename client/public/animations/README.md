# Rive animations

Put your exported `.riv` files here. Recommended hero path:

```txt
public/animations/readnest-reader.riv
```

Then set this in `client/.env`:

```txt
VITE_RIVE_READER_SRC=/animations/readnest-reader.riv
VITE_RIVE_READER_STATE_MACHINE=State Machine 1
```
